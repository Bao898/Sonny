<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <ResourceDictionary.MergedDictionaries>
        <!-- MaterialDesign -->
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TabControl.xaml" />

        <!-- Custom Styles -->
        <ResourceDictionary Source="SonnyColors.xaml" />
        <ResourceDictionary Source="GeneralStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Define HeaderTemplate in a separate DataTemplate resource -->
    <DataTemplate x:Key="TabItemHeaderTemplate">
        <StackPanel Orientation="Horizontal">
            <!--<materialDesign:PackIcon
              Width="24" Height="24" VerticalAlignment="Center"
              Kind="Group" />-->
            <TextBlock Text="{Binding}"
                     FontFamily="{DynamicResource SonnyBIM_TitleFontFamily}"
                     FontSize="14"
                     FontWeight="SemiBold"
                     Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
        </StackPanel>
    </DataTemplate>

    <!-- TabControl Style -->
    <Style x:Key="SonnyBIM_TabControlDefaultStyle" TargetType="TabControl"
         BasedOn="{StaticResource MaterialDesignTabControl}">
        <Setter Property="Background" Value="White"/>
        <!--<Setter Property="Padding" Value="0"/>-->
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="TabStripPlacement" Value="Top"/>
        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
        <Setter Property="materialDesign:ColorZoneAssist.Mode" Value="Standard"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- TabPanel (Header Container) -->
                        <Border Grid.Row="0"
                        BorderThickness="0,0,0,1"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Background="{TemplateBinding Background}">
                            <TabPanel x:Name="HeaderPanel"
                            IsItemsHost="True"
                            KeyboardNavigation.TabIndex="1"
                            Panel.ZIndex="1"
                            Margin="0,0,0,0"/>
                        </Border>

                        <!-- Tab Content -->
                        <Border Grid.Row="1"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                   ContentSource="SelectedContent"
                                   Margin="0"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style x:Key="SonnyBIM_TabItemDefaultStyle" TargetType="TabItem"
         BasedOn="{StaticResource MaterialDesignTabItem}">
        <!--<Setter Property="Background" Value="#F5F5F5"/>-->
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <!--<Setter Property="Foreground" Value="{DynamicResource MauPhuSonnyBIM}"/>-->
        <Setter Property="Background" Value="{DynamicResource SonnyBIM_MauNenDataGrid}"/>
        <Setter Property="Margin" Value="0,0,1,0"/>
        <Setter Property="Padding" Value="10,8,10,5"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid Background="{TemplateBinding Background}">
                        <Border Name="Border"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                            ContentSource="Header"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MauTitleBarSonnyBIM}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource MauChinhSonnyBIM}"/>
                            <Setter Property="Foreground" Value="{DynamicResource MauPhuSonnyBIM}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                        <!--<Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource MauTitleBarSonnyBIM}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource MauPhuSonnyBIM}"/>
                        </Trigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl With Underline Style -->
    <Style x:Key="SonnyBIM_TabControlUnderlineStyle" TargetType="TabControl"
         BasedOn="{StaticResource SonnyBIM_TabControlDefaultStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <!--<Setter Property="Padding" Value="0"/>-->
        <Setter Property="Margin" Value="0"/>
    </Style>

    <!-- TabItem With Underline Style -->
    <Style x:Key="SonnyBIM_TabItemUnderlineStyle" TargetType="TabItem"
         BasedOn="{StaticResource MaterialDesignTabItem}">
        <Setter Property="Background" Value="#F5F5F5"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource MauPhuSonnyBIM}"/>
        <!--<Setter Property="Padding" Value="16,10,16,10"/>-->
        <Setter Property="Margin" Value="0,0,1,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid Background="{TemplateBinding Background}">
                        <Border Name="Border"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                            ContentSource="Header"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MauTitleBarSonnyBIM}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource MauChinhSonnyBIM}"/>
                            <Setter Property="Foreground" Value="{DynamicResource MauPhuSonnyBIM}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Trigger>
                        <!--<Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource MauTitleBarSonnyBIM}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource MauPhuSonnyBIM}"/>
                        </Trigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
